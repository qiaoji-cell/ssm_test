<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.axjy.mapper.sys.RolePermissionMapper">

  <delete id="delRolePermissionByRole">
    delete from kms_role_permission where role_id = #{roleId}
  </delete>

  <delete id="delRolePermissionByRoles">
    delete from kms_role_permission where role_id in
    <foreach collection="array" item="roleId" open="(" separator="," close=")">
        #{roleId}
    </foreach>
  </delete>

  <insert id="addRolePermissionByRole">
    insert into kms_role_permission (role_id, perm_id) values
    <foreach collection="perIds" item="permId" separator=",">
      (#{roleId},#{permId})
    </foreach>
  </insert>

  <select id="findPermIdByRole" resultType="java.lang.Long">
    select perm_id from kms_role_permission where role_id = #{roleId}
  </select>

  <insert id="batchAdd">
    insert into kms_role_permission(role_id,perm_id) values
    <foreach collection="idList" item="permId" separator=",">
      (#{roleId},#{permId})
    </foreach>

  </insert>

  <delete id="deleteByPermId">
    delete from kms_role_permission where perm_id = #{permId}
  </delete>

  <select id="countChild" resultType="java.lang.Integer">
    select count(0) from kms_role_permission where perm_id in
    <foreach collection="list" item="perm" open="(" separator="," close=")">
        #{perm.id}
    </foreach>
  </select>

  <!--插入父菜单权限的的id-->
  <insert id="insertRoleParent">
    insert into kms_role_permission(role_id,perm_id) values(#{roleId},#{parentId})
  </insert>

  <select id="countParent" resultType="java.lang.Integer">
    select count(0) from kms_role_permission where perm_id = #{parentId}
  </select>

  <!--批量删除角色权限关系-->
  <delete id="batchDelPerm">
    delete from kms_role_permission where perm_id in
    <foreach collection="list" item="perm" open="(" separator="," close=")">
      #{perm.id}
    </foreach>
  </delete>
  <resultMap id="BaseResultMap" type="com.axjy.pojo.sys.RolePermission">
    <id column="id" jdbcType="BIGINT" property="id" />
    <result column="role_id" jdbcType="BIGINT" property="roleId" />
    <result column="perm_id" jdbcType="BIGINT" property="permId" />
  </resultMap>
  <sql id="Base_Column_List">
    id, role_id, perm_id
  </sql>
  <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
    select 
    <include refid="Base_Column_List" />
    from kms_role_permission
    where id = #{id,jdbcType=BIGINT}
  </select>


    <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
    delete from kms_role_permission
    where id = #{id,jdbcType=BIGINT}
  </delete>

  <insert id="insert" keyColumn="id" keyProperty="id" parameterType="com.axjy.pojo.sys.RolePermission" useGeneratedKeys="true">
    insert into kms_role_permission (role_id, perm_id)
    values (#{roleId,jdbcType=BIGINT}, #{permId,jdbcType=BIGINT})
  </insert>
  <insert id="insertSelective" keyColumn="id" keyProperty="id" parameterType="com.axjy.pojo.sys.RolePermission" useGeneratedKeys="true">
    insert into kms_role_permission
    <trim prefix="(" suffix=")" suffixOverrides=",">
      <if test="roleId != null">
        role_id,
      </if>
      <if test="permId != null">
        perm_id,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides=",">
      <if test="roleId != null">
        #{roleId,jdbcType=BIGINT},
      </if>
      <if test="permId != null">
        #{permId,jdbcType=BIGINT},
      </if>
    </trim>
  </insert>
  <update id="updateByPrimaryKeySelective" parameterType="com.axjy.pojo.sys.RolePermission">
    update kms_role_permission
    <set>
      <if test="roleId != null">
        role_id = #{roleId,jdbcType=BIGINT},
      </if>
      <if test="permId != null">
        perm_id = #{permId,jdbcType=BIGINT},
      </if>
    </set>
    where id = #{id,jdbcType=BIGINT}
  </update>
  <update id="updateByPrimaryKey" parameterType="com.axjy.pojo.sys.RolePermission">
    update kms_role_permission
    set role_id = #{roleId,jdbcType=BIGINT},
      perm_id = #{permId,jdbcType=BIGINT}
    where id = #{id,jdbcType=BIGINT}
  </update>
</mapper>